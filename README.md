# wyred-contract — the versioned emit contract: schemas + golden artifacts. The product's real API; everything depends on this, this depends on nothing.

Contract v0 == elecscad harness EMIT_CONTRACT v3, adopted verbatim. goldens/ga019/ are real artifacts from ga019's final gate-green run (Watchy, MPPT, probe corpus).
